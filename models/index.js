const candidate = require('./candidate');
const municipality = require('./municipality');
const municipality_mimage = require('./municipality_mimage');

module.exports = {
  candidate,
  municipality,
  municipality_mimage};
